# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: Find and Replace
        uses: jacobtomlinson/gha-find-replace@v3
        with:
          find: "base: '/pages'"
          replace: "base: '/'"
          include: "src/config.yaml"
          regex: false  

      - name: 🔨 Build Project
        env:
          PUBLIC_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
        run: |
          echo "Environment Variable: $PUBLIC_FIREBASE_API_KEY:"
          npm install
          npm run astro build

      - name: 📂 Deploy to firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_EVANG9_COMBO_4CB8E }}
          channelId: live
          projectId: evang9-combo-4cb8e
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks  
