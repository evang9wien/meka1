---
import Layout from '~/layouts/PageLayout.astro';
import Login from '~/components/svelte/auth/Login.svelte';

const metadata = {
  title: 'Login',
};

const loginReload = false;
---

<Layout metadata={metadata}>
  <Login client:only="svelte" {loginReload} />
</Layout>
