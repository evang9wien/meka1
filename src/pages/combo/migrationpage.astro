---
import Layout from '~/layouts/PageLayout.astro';
import MigrationTool from '~/components/svelte/MigrationToolFB.svelte';

const metadata = {
  title: 'Migration: mitarbeiter → accounts',
};
---

<Layout metadata={metadata}>
  <MigrationTool client:load />
</Layout>
