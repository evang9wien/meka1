---
import Layout from '~/layouts/PageLayout.astro';
import NewsletterPart from '~/parts/newsletter/newsletterpart.astro';

const metadata = {
  title: 'Newsletter',
};
---

<Layout metadata={metadata}>
  <NewsletterPart />
</Layout>
